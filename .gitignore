# Environment — contains credentials, never commit
.env

# rclone config — contains OAuth tokens (keep the dir, not the secrets)
config/rclone/*
!config/rclone/.gitkeep

# qBittorrent config — contains credentials + state
config/qbittorrent/*
!config/qbittorrent/.gitkeep

# Browser config — Chromium runtime state (ignored except our custom params)
config/browser/*
!config/browser/.gitkeep
!config/browser/chromium-params.sh

# Downloaded files
downloads/incomplete/*
downloads/completed/*
!downloads/incomplete/.gitkeep
!downloads/completed/.gitkeep

# Upload logs
logs/*
!logs/.gitkeep

# Cookies exports — contain session tokens, never commit
*_cookies.txt
config/cookies/*
!config/cookies/.gitkeep

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp
